# ISerialChannel - Header-only interface for serial communication channels
# Provides abstract interface for polymorphic serial handlers (UART, USB CDC, TCP, mocks)

idf_component_register(
    INCLUDE_DIRS "include"
)
