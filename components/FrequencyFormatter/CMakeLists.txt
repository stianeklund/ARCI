# FrequencyFormatter - Header-only component for CAT frequency formatting
# Provides zero-allocation, high-performance frequency formatting utilities

idf_component_register(
    INCLUDE_DIRS "include"
)
